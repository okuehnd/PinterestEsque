import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3f51b5',  // Primary blue color
    },
    secondary: {
      main: '#59FFA0',  // Teal for secondary color
    },
  },
});

export default theme;